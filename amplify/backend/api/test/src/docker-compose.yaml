version: '3'
services:
  app:
    build: .
    volumes:
      - .dockervenv:/src/.venv
      - .:/src
    networks:
      - public
      - private
    restart: always
    tty: true
    ports:
      - 80:80
networks:
  public:
  private: