version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend/front
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/front/build
    files:
      - "**/*"
  cache:
    paths: []
